for windows you need
  3Dconnexion 3DxWare (SDK)
and in this directory
  si.h, siapp.c, siapp.h, spwdata.h, spwerror.h, spwmacro.h, version.h, sbtest32.h
and the driver must run

for unix you need
  xdriver, xdrvlib
and in this directory
  xdrvlib.h, xdrvlib.c
and the driver must run
